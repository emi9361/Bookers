
<h1><b>Editing Book<b></h1>

<%= form_for (@book) do |f| %>

  <h4>Title</h4>
<%= f.text_field :title %>

  <h4>Body</h4>
<%= f.text_area :body %>

<%= f.submit '保存' %>

<% end %>

<%= link_to "Show", book_path(@book.id) %>
<%= link_to "Back", books_path %>